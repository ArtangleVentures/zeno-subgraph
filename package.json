{
  "name": "zeno-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zeno-subgraph",
    "create-local": "graph create --node http://localhost:8020/ zeno-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ zeno-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.35.1",
    "zeno-subgraph": "file:"
  }
}
